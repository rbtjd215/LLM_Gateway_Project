# LLM Security Gateway 성능 및 보안 테스트 보고서

본 문서는 LLM 게이트웨이의 V4 하이브리드 파이프라인(정규식 + 로컬 LLM)에 대한 성능, 방어율, 지연 시간 벤치마크 테스트 결과를 요약한다.

---

## 1. 테스트 개요

* **목적:** 기존 정규식 기반(V1) 솔루션 대비 하이브리드(V4) 모델의 방어율 향상 입증 및 프롬프트 인젝션 방어 능력 측정
* **데이터셋 규모:** 총 1,200건의 자동화 스크립트 질의 (정상 질의, 기밀 유출, 인젝션 공격, 우회 공격 등 혼합)
* **테스트 환경:** 일반 기업용 랩탑 (Samsung Galaxy Book 6 Pro 16")
  * **CPU:** Intel Core Ultra 7 358H (Panther Lake)
  * **GPU:** Intel Arc B390 내장 그래픽
  * **RAM:** 32GB LPDDR5X (CPU/GPU 통합 메모리 구조)
* **테스트 모델:** 로컬 `qwen2.5:7b` (Ollama 구동)

---

## 2. 보안 품질 지표 (Quality Metrics)

1,200건의 자동화 테스트를 통해 도출된 핵심 성능 지표는 다음과 같다.

| 평가 지표 | V1 (정규식 전용) | **V4 (하이브리드 파이프라인)** | 성과 요약 |
|---|:---:|:---:|---|
| **종합 방어율 (Recall)** | 26.7% | **78.33%** | 🔺 **약 2.9배 향상** |
| **우회 공격 탐지율** | 0.0% | **51.00%** | 🔺 새로운 방어선 확보 |
| **인젝션 차단율** | ~15% | **91.67%** | 🔺 **약 6배 향상** |
| **정밀도 (Precision)** | ~100% | **99.86%** | 🔹 정상 업무 방해 거의 없음 |
| **오탐률 (FPR)** | 0.0% | **0.08%** (1,200건 중 1건) | 🔹 실무 투입 가능한 수준 |

### 언어별 상세 품질 지표 (Language-specific Metrics)

| 언어 | Precision | Recall | Accuracy | 오탐률 (FPR) |
|---|---:|---:|---:|---:|
| **English** | 99.72% | 79.33% | 84.33% | 0.28% |
| **Korean** | 100.00% | 77.33% | 83.00% | 0.00% |

> **분석:** 전반적인 오탐률(FPR)이 평균 0.14% 수준(한국어 0%, 영어 0.28%)으로 매우 낮다. 이는 강력한 Fail-Closed 정책을 적용했음에도 불구하고, 정상적인 업무 질의를 위협으로 오인하여 차단하는 현상(업무 방해)이 극히 드물게 발생함을 의미하며 실무 투입에 적합한 가용성을 입증한다.

---

## 3. 시스템 응답 속도 벤치마크 (Latency) 및 인프라 확장성 입증

무거운 LLM을 사내 보안 시스템으로 도입할 때 가장 큰 걸림돌은 지연 시간(Latency)이다. LLM Gateway는 **비동기 병렬 처리(`asyncio.gather`)**를 통해 이 오버헤드를 대폭 단축하도록 설계되었다.

### 3.1. [환경 A] CPU 단독 연산 환경 (GPU 미사용)
* **환경:** CPU 단독 연산 (Intel Arc GPU 가속 비활성화)

| 질의 유형 | 초기 구동 (Cold Start) | **실제 운영 (Warm State)** |
|---|:---:|:---:|
| **정상 질의** | 31.43s ~ 52.85s | **1.10s ~ 9.73s** |
| **기밀 포함 (마스킹)** | 30초 이상 | **1.25s ~ 3.53s** |
| **공격 시도 (인젝션)** | 30초 이상 | **1.92s ~ 3.10s** |

> **분석:** 로컬 모델이 RAM에 적재되는 최초 1회(Cold Start)에는 극심한 지연이 발생하나, 일단 Warm State에 진입하면 평균 1~3초대(최대 9.7초)의 처리 속도를 보여준다.

### 3.2. [환경 B] GPU 구동 환경 (확장성 증명)
추가로 하드웨어 제약이 해소되었을 때의 잠재력을 확인하기 위해, 동일한 아키텍처(코드 수정 없음)에서 **인텔 Arc GPU 가속(Offloading)**을 활성화하여 추가 테스트하였다.

| 질의 환경 | 초기 구동 (Cold Start) | **실제 운영 (Warm State)** |
|---|:---:|:---:|
| **GPU 가속 환경** | 즉각 적재 (단축됨) | **평균 1.36초** |

* **결과 상세:** 1,200건의 가혹 조건 대규모 벤치마크를 순차적으로 실행했음에도 불구하고, 콜드 스타트 오버헤드를 모두 포함하여 1건당 평균 1.36초만에 처리를 완료하였다.

> **결론 및 시사점:** 
> CPU 구동 시 관찰되었던 지연 시간 한계는 구조적 결함이 아님이 입증되었다. 예상한 대로 **인프라 자원(GPU)을 투입하는 만큼 성능(처리 속도)이 비약적으로 올라갈 수 있다는 것(Scalability)을 증명**하였다.

---

## 4. 한계 분석 및 향후 과제

본 프로젝트의 구조적 우수성에도 불구하고, 소형 모델 사용으로 인한 한계점을 명확히 인지하고 분석했다.

### 한계점: 78.33%의 방어율
78.33%의 방어율은 **위협의 약 21.67%를 놓친다**는 것을 의미한다.
* **원인 분석:** 70억(7B) 파라미터 수준의 소형 모델은, 공격자가 키릴 문자를 섞어 쓰거나 한국어 초성 변환 우회("비밀번호 알려줘" -> "ㅂㅁㅂㅎ ᄋrㄹㅕ줘") 등 매우 고도화된 우회 공격을 시도할 경우, 문맥을 완벽히 유추해 내는 데 한계를 보였다.

### 결론: 엔진의 한계일 뿐, 뼈대의 결함이 아니다
이러한 미탐지는 본 연구가 제안한 3단계 하이브리드 아키텍처의 결함이 아니라, **7B 소형 모델 자체의 추론 한계**이다.
* 99.86%의 정밀도는 방어 아키텍처(자동차의 뼈대)가 매우 견고하게 설계되었음을 입증한다.
* 향후 고성능 GPU 서버를 도입하여 **70B 이상의 대형 모델(고성능 엔진)로 교체하기만 하면, 방어율은 즉각적으로 90% 이상으로 수직 상승**할 수 있는 확장성 높은 구조이다.

---

## 5. 보안 테스트 실행 방법 (How to Run Tests)
본 프로젝트는 단위 테스트부터 1,200건의 대규모 QA 벤치마크까지 다양한 자동화 테스트 스크립트를 제공한다.
해당 스크립트들은 본 문서의 성능 지표를 도출하는 데 사용된 핵심 파일들이므로 **절대 삭제해서는 안 된다.**

### 5.1 기본 보안 로직 단위 테스트
정규식 마스킹 및 Ollama 연동에 대한 기본 보안 단위 테스트를 실행한다.
```bash
# 가상환경 활성화 후
pip install pytest
pytest tests/test_security.py -v
```

### 5.2 전체 테스트 스위트 실행
E2E 테스트, 응답 품질, 인젝션 방어 등 파이프라인 전체 단계를 한 번에 검증한다.
```bash
pytest tests/ -v
```

### 5.3 대규모 QA 벤치마크 스크립트 (`scripts/` 폴더)
본 문서의 '보안 품질 지표' 표에 나오는 1,200건의 대규모 벤치마크 결과를 직접 재현하고 분석하려면 다음 스크립트들을 사용한다.
```bash
# 한국어/영어 대규모 테스트 셋 실행 및 분석
python scripts/run_korean_test.py
python scripts/analyze_final.py
```

---

### 전체 문서 목차
1. [**메인 페이지 (README)**](../README_KO.md)
2. [**시스템 아키텍처**](./ARCHITECTURE.md)
3. [**설치 및 실행 가이드**](./SETUP_GUIDE.md)
4. [**보안 테스트 가이드**](./SECURITY_TESTING.md) 🔴 *현재 페이지*
5. [**API 명세서**](./API_REFERENCE.md)
